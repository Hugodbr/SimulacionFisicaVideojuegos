#pragma once

#include "MeshSystem.h"

class UniformParticleGenerator;

class CurveSystem : public MeshSystem
{
public:
    CurveSystem() = delete;
    // CurveSystem(const std::string& filename);
    // CurveSystem(const std::string& filename, float pointSize, double scale, const Vector4& color);

    // void init() override;
    // void update(double deltaTime) override;

protected:

};
