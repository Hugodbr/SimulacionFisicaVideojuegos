{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Rebuild Project",
            "type": "shell",
            "command": "${workspaceFolder}\\rebuild.bat",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "detail": "Runs rebuild.bat to clean, configure, and build the project"
        }
    ]
}